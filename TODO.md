# TODO

## Media Support

- [ ] `tome show` — render inline images as Sixel between text blocks (via `img2sixel`)
- [ ] `tome attachments "note name"` — extract attachments to temp dir, optionally display as Sixel
- [ ] `tome edit` — warn/guard when a note has attachments to avoid nuking them (add `--force` flag to override)
